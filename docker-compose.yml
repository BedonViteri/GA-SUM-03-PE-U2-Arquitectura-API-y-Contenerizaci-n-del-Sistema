services:

  auth-db:
    image: postgres:16
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=auth_db
    volumes:
      - auth-db-data:/var/lib/postgresql/data
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d auth_db"]
      interval: 5s
      timeout: 5s
      retries: 10

  resource-db:
    image: postgres:16
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=resource_db
    volumes:
      - resource-db-data:/var/lib/postgresql/data
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d resource_db"]
      interval: 5s
      timeout: 5s
      retries: 10

  notification-db:
    image: postgres:16
    environment:
      - POSTGRES_USER=sga_user
      - POSTGRES_PASSWORD=sga_pass
      - POSTGRES_DB=notifications_db
    volumes:
      - notification-db-data:/var/lib/postgresql/data
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sga_user -d notifications_db"]
      interval: 5s
      timeout: 5s
      retries: 10


  auth-service:
    build: ./auth-service
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXP_MIN=${JWT_EXP_MIN}
      - DATABASE_URL=postgresql://postgres:postgres@auth-db:5432/auth_db
    depends_on:
      auth-db:
        condition: service_healthy
    networks:
      - backend
    restart: on-failure

  notification-service:
    build: ./notification-service
    environment:
      - DATABASE_URL=postgresql://sga_user:sga_pass@notification-db:5432/notifications_db
    depends_on:
      notification-db:
        condition: service_healthy
    networks:
      - backend
    restart: on-failure

  resource-service:
    build: ./resource-service
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - DATABASE_URL=postgresql://postgres:postgres@resource-db:5432/resource_db
      - NOTIFICATION_URL=http://notification-service:8003
    depends_on:
      resource-db:
        condition: service_healthy
      notification-service:
        condition: service_started
    networks:
      - backend
    restart: on-failure
  
  api-gateway:
    image: nginx:alpine
    ports:
      - "8080:8080"
    volumes:
      - ./api-gateway/nginx.conf:/etc/nginx/nginx.conf:ro
      - gateway-logs:/var/log/nginx
    depends_on:
      - auth-service
      - resource-service
      - notification-service
    networks:
      - backend

  
  portainer:
    image: portainer/portainer-ce:latest
    ports:
      - "9000:9000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer-data:/data
    networks:
      - backend


networks:
  backend:
    driver: bridge

volumes:
  auth-db-data:
  resource-db-data:
  notification-db-data:
  gateway-logs:
  portainer-data: